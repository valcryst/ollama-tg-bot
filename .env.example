# Required — from @BotFather
BOT_TOKEN=your_telegram_bot_token_here

# Optional
PORT=3000
DATABASE_PATH=./data/bot.db

# Docker / Portainer: set BOT_TOKEN in stack environment variables,
# or copy this file to .env next to docker-compose.yml on the host.
